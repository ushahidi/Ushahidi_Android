<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/background_color"
    android:orientation="vertical" >

    <LinearLayout style="@style/ActionBarTitleContainer" >

        <ImageButton
            android:id="@+id/list_map_toolbar_add"
            style="@style/ActionBarTitle"
            android:src="@drawable/button_add" />

        <ImageView style="@style/ActionBarTitleSeparator" />

        <TextView
            android:id="@+id/list_map_toolbar_title"
            style="@style/ActionBarTitleText"
            android:gravity="center"
            android:text="@string/maps" />
        <ImageView style="@style/ActionBarTitleSeparator" />

        <ImageButton
            android:id="@+id/list_map_refresh_btn"
            style="@style/ActionBarTitle"
            android:src="@drawable/button_refresh" />

        <ProgressBar
            android:id="@+id/list_map_refresh_progress"
            style="@style/TitleBarProgressIndicator"
            android:visibility="gone" />
    </LinearLayout>

    <ImageView style="@style/ActionBarSeparator" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="6dip" >

        <!-- Holds the found deployments -->

        <ListView
            android:id="@android:id/list"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:fadingEdge="none"
            android:fastScrollEnabled="true"
            android:scrollbars="vertical"
            android:scrollingCache="true"
            android:smoothScrollbar="true" />
    </LinearLayout>
    <!-- Here is the view to show if the list is emtpy -->

    <TextView
        android:id="@+id/empty_list_for_maps"
        style="@style/TextView.Empty"
        android:text="@string/no_items" />
    <!-- progress bar -->

    <ProgressBar
        android:id="@+id/map_refresh_progress"
        style="@style/ListBarProgressIndicator"
        android:visibility="gone" />

</LinearLayout>